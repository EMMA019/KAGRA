# Prompt

Close fighting remaining gaps on origin/master (94db280): guard + 2-hit combo.

Hold Shift/C/K (WalkInput.dodge) blocks incoming hits (no damage / no KO), dump-visible. Two attacks in a window register as a combo when the first landed, dump-visible. Keep hitstun / KO / hit flash. Existing fight_hitstun_world.json fixture. No new ECS / RendererV2 / VRM / Rapier / SSAO. world_play.rs stays tiny.

