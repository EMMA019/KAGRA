# Prompt

Continue overnight local KAGRA. This slice only: raise SHARED wgpu 30 picture (density/light/material).

Start FROM local/m3-stealth-hide. Branch local/m3-shared-picture. Do NOT merge origin/master. No force-push.

Picture is NOT a separate mountain. Raise the shared wgpu 30 WorldDoc path that play_world already uses. High quality (density/light/material) is not abandoned.

Do NOT cut a mountain by Unreal feature names. Do NOT split a second HQ renderer. Do NOT add picture on RendererV2. Do NOT port VRM skin. Do NOT add SSAO, Rapier, GI, or a 0.19+30 mix.

Land concrete shared compile_scene/WorldDoc improvements that every genre dump inherits.