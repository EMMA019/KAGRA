# Session — local lights 4 slots

- #61 マージ直後。室内画素は CI `golden` 待ち。閉じない。
- 這いの画素はカメラが動くと全体が動くので、このスライスではやらない。
- 局所 4 は `slot=0..3`。0 がキー（スポットがマップを所有可）。1..3 は埋め、影なし。
- Camera uniform 288 → 448。既存のスロット 0 オフセットは維持。
- Pretty Room はスモーク以外で埋め 2。Garden スモークは触らない。
- 剛体（Rapier）はこのスライスに入れない。
