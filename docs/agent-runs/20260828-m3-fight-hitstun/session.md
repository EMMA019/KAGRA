# Session

Started from `local/m3-race-drive` (`6a7c6e9`). Created `local/m3-fight-hitstun`.
Attack / facing / stun / KO live in `fight.rs`. WorldPlay only dispatches. Ring dump is `fight_hitstun_world.json` (two capsules, ring floor, 4 lights, dual camera).
