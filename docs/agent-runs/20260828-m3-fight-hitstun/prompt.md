# Prompt

Continue overnight local KAGRA. Branch `local/m3-fight-hitstun` from `local/m3-race-drive`.

This slice only: fighting slice 1 -- attack + hitstun + KO.
Two capsules, J/click attack, hitstun, KO/retry dump-visible. Title->play->result reuses WorldPlay. Sibling `fight.rs`. world_play.rs touch tiny. Capsules, not VRM. Camera keeps both bodies in view. Hit flash overlay.

NOT: combo editor, specials, net, novel, vehicles, tower UI, RendererV2, SSAO/GI, new ECS. Do not merge master. No force-push.
