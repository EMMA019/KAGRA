# Session

Started from `local/m3-td-path-spawn` (`3454a35`). Created `local/m3-race-drive`.
Kinematic steer+throttle lives in `race.rs`. WorldPlay only dispatches. Track dump is `race_drive_world.json` (road boxes, finish, split, flag, box car, 4 lights).
