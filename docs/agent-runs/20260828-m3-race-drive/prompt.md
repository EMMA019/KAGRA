# Prompt

Continue overnight local KAGRA. Branch `local/m3-race-drive` from `local/m3-td-path-spawn`.

This slice only: racing slice 1 — kinematic drive + lap/finish.
WASD/arrows steer+throttle a kinematic body on a simple track (boxes). Finish/lap dump-visible. Title→play→result reuses WorldPlay. Sibling `race.rs`. world_play.rs touch tiny. Capsule/box car, not Rapier, not VRM. Camera follows the car. 4 light slots.

NOT: Rapier vehicles, drifting physics, net, fighting, novel, tower placement UI, RendererV2, SSAO/GI, new ECS. Do not merge master. No force-push.
