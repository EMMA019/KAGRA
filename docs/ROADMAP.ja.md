# KAGRA ロードマップ — 体と、その体が歩ける部屋

最終更新: 2026-08-24（見直し: 頭脳を Stage 0.5 として最優先。#54 整理の上。
PyPI は 0.1.3。能力の棚卸しは [REVIEW.ja.md](REVIEW.ja.md)）。

## 最終目標（北極星）

**「Python で AI に体を与えるなら KAGRA」という第一想起。**
JS における three-vrm のポジションの Python 版。

これはエンジンの機能数ではない。測るのは Pull:

- 投稿していない日に install と「動いた」報告が来るか（オーガニック比率）
- 外部発のコンテンツ（記事・動画・配信）が毎月出るか

売るのは完成品（アバターがいる、AI が配信する）。ライブラリの需要はその裏から流れる。
Web で three-vrm と戦わない。pygame の土俵にも上がらない。

到達の形は Stage 2〜5（コード不要の完成品 → 旗艦の公開運用 → 他の需要に体を刺す → 複利）。
Stage 1 の楔は、どの完成品が本物の痛みかを数字で選ぶためにある。

## 地図（今どこで、次は何か）

```
基礎（P0–P8 + 部屋 v1）     完了。「置いて歩ける」
Stage 0.5 頭脳面            最優先。楔 A/B の前提。画素と並行
使える週                     進行中。API は #52。画素 + 法線 + パッド
  → 3 見本が 30 秒テストを通る（楔 D のゲート）
PyPI                         ソースの usable-week を wheel に載せる
Stage 1 楔の同時投下         頭脳 + 3 見本のあと（C はワンライナー、優先低）
Stage 2〜5                   勝ち楔を完成品・旗艦・供給・複利へ
その先のエンジン             禁止ではない。今のバーの外。指示で足す
```

エンジンの「three.js の普段 + Ursina で書く週」は **今のバー** であって最終目標ではない。
Rapier / OSM / 4 段 CSM / SSAO などは「このバーでは後回し」。永久にやらない、ではない。

## 次にやること（この順）

1. **頭脳面** — `kagra.brain`（Ollama / OpenAI 互換 / kairi を 1 面）+ `docs/recipes/ai-brain.md` + 15 行の見本。コアにモデルを入れない
2. **絵の残り** — 法線マップ → 室内影の画素確認 → トーンマップの画素。golden / smoke スクショで閉じる。実機パッドは同じバー
3. **PyPI** — usable-week API を次の wheel に載せる。ソースだけの状態を長くしない。macOS wheel は CI で取る
4. **D-6** — 3 見本の 30 秒テストのあと。箱の焼き直しではない。30 秒以上遊べて、スコアか明確なゴールがある
5. **楔 C** — マスコットは既にある。ワンライナー / 配布物を整える。エンジン不足ではない。優先は下
6. **Stage 1** — 楔を同時に投げる。A/B は頭脳のあと。D は D-6 のあと。C は 5 が済めば投げてよい

## 4 原則

1. **需要は完成品から逆流する。** 「エンジンが欲しい」ではない。
   「デスクトップに自分のアバターがいる」「寝てる間に AI が配信してる」が欲しい。
2. **Push は検証手段であって需要ではない。** 投稿した日の star は反応。
   投稿していない日の流入が需要。
3. **楔は同時に打ち、勝者を数字で選ぶ。** 同じ労力で最大 4 本。
   D を箱部屋の初日品質で投げない（「作るものがしょぼい」と読まれる）。
4. **今詰まっている楔から足す。** ただし「今やらない」を天井にしない。
   棚の 2D / エディタ / tilemap は Front に戻さない（製品の決めごと）。
   頭脳は楔 A/B が詰まっているので今足す。使える週の画素は楔 D が詰まっているので並行する。
   P9 や Rapier を先にマージして「次は頭脳」を飛ばさない。

## 完了の単位

API が生えたことは完了ではない。次を満たしたらチェックする。

| 見本 | 見知らぬ人に 30 秒見せたとき |
|---|---|
| Pretty Room | three.js の室内例の隣に置いて「エンジンのデバッグ画面」に見えない。スポットが影を落とし、金属が環境を映す |
| Overworld | 高さ場の単体テストに見えない。屋外の影が這わず、拾うものがあり、ジャンプがゲームに感じる |
| Prop Garden | ロックしてクリックして、何かが動く。1 分遊んで戻したくならない最低限 |
| D-6 | エージェントが **Prop / Walk / 使える週の API だけ** で書く。箱が落ちる・レーン・スイッチの焼き直しではない。**30 秒以上遊べて、スコアか明確なゴールがある**（Overworld 収集、Garden 拡張など） |

3 つの見本が揃うまで D-6 を投げない。頭脳は D のゲートではない（A/B のゲート）。

## 何で戦うか

技術的な優位は「点」ではなく「縦の貫通」。

1. **統合密度 × 配布**: 依存ゼロ・約 5MB wheel で、glTF → MToon → SpringBone →
   リップシンク → TTS → OBS 仮想カメラが 1 プロセス。競合はだいたいブラウザ / Godot に描画を投げている
2. **VRM 実装深度**: マルチスキン、コライダー付き SpringBone、node constraint、
   expression override、一人称レイヤー
3. **エージェント開発ループ**: `kagra.verify` / golden / `docs/API_INDEX.md` / MCP。
   ループがあっても、出せるゲームが初日ならエージェントは来ない

比較表の全文は [REVIEW.ja.md](REVIEW.ja.md)。

---

## Stage 0 — 現在地

- [x] 0.1.3 PyPI（Windows / Linux、約 5MB、Rust 不要）— **ソース `master` の方が先**
- [x] VRM 0.x / 1.0 一式・リップシンク・仮想カメラ・3D 物理・顔トラ
- [x] VTuber 面の絵作り: 空・リム・showcase・接地。`python -m kagra` がデバッグ画面に見えない
- [x] 基礎（P0–P8 + 部屋 v1）: カリング / インスタンス / ワールド影 / 点・スポット /
      HDRI + irradiance / 金属粗さ / `Prop` `Walk` `sky` `room` / 島 / 街 JSON /
      三角形当たり / AABB 積み木 / 2 段影（既定 1）
- [x] 使える週の **API**（#52）: `set_tonemap` / スペキュラ mip / スポット透視影 /
      テクセルスナップ / ポインタロック / `clicked_prop` / `animate` / `Label` /
      `Walk.carry` / coyote / 親子 2 段 / `sound`。スナップテスト修正 #53
- [ ] 使える週の **見本**: Pretty Room / Overworld / Prop Garden が 30 秒テストを通る。
      画素は GPU / CI スクショでまだ閉じていない。法線と USB パッドは未着手
- [ ] **Stage 0.5 頭脳面（最優先）:** `kagra.brain` + `docs/recipes/ai-brain.md`。
      あるのは棚の `AiCharacter`
- [ ] 次の PyPI: usable-week API を wheel に載せる。macOS wheel は CI
- [x] 初回体験: own-VRM 1 行、シャドウ警告、`kagra.cmd`、レシピ、issue テンプレ

**VTuber / マスコットの最初のユーザーを取る絵は足りている。楔 A/B は頭脳が無いと打てない。**
**ゲームを書く入口は API まで来た。画素がバーを閉じ、D-6 が箱から出る。**

---

## 能力トラック

### 基礎（初日。完了）

「置いて歩ける」。チェックを増やして満足しない。

### Stage 0.5 — 頭脳（楔 A/B の前提。最優先）

画素と並行してよい。D のゲートにはしない。コアに torch / 重量級モデルを入れない。

- [ ] `kagra.brain` — Ollama / OpenAI 互換 / kairi を 1 面で切替
- [ ] `docs/recipes/ai-brain.md` + 15 行の見本（`examples/vrm_kairi_chat.py` または ollama 相当）
- [ ] 棚の `AiCharacter` は Front に上げない。新しい面に寄せてから畳む

### 使える週 — 今のバー（three.js の普段 + Ursina で書く週）

一つのバー。API を載せてもチェックしない。チェックは見知らぬ人に 30 秒見せてから。
「級」は 3 見本が通るまで使わない。楔 D / D-6 のゲート。

**ソースに載った（#52）。画素未確認:**

絵の残りはこの順: **法線マップ → 室内影の画素 → トーンマップの画素。**

- [ ] **法線マップ。** `Prop` / `upload_mesh_3d` / glTF `normalTexture`（未着手）
- [ ] **室内の影。** スポットが同じ 2048 マップに透視影。画素で見える
- [ ] **トーンマップ。** ACES。既定オフ。Pretty Room / Overworld が opt-in。白飛びが止まる
- [ ] **スペキュラ IBL。** キューブ 4 mip + `textureSampleLevel`。金属がプラスチックに見えない
- [ ] **屋外の影が這わない。** 2 段のテクセルスナップ（#53 でテスト境界を修正）
- [ ] **ポインタロック。** 一人称。OS が拒めばフォールバック
- [ ] **クリック。** `clicked_prop`。消すデモではなく使う / 持つ
- [ ] **`animate` / Sequence**
- [ ] **HUD。** `Label` / `Button`（画面空間。2D `kagra.ui` は棚）
- [ ] **持つ。** `Walk.carry`
- [ ] **ジャンプ。** coyote + バッファ
- [ ] **親子 2 段。** 孫まで
- [ ] **音の一行。** `sound("coin")`
- [ ] **パッドの実機。** USB/XInput。`inject_pad` はある（未着手）

### その先のエンジン（今のバーの外。禁止ではない）

指示があれば足す。0.5 と使える週を閉じる前に勝手に始めない、という順番だけ。

絵: 4 段 CSM、SSAO、ボリュメトリック、複数ディレクショナル、ディスプレースメント、
フル PMREM、kagra-shared とのレンダラ統合。
操作: 深いシーングラフ、ナビメッシュ、インベントリ、Tween カタログ全部、
アニメーションステートマシン、スキン Prop。
世界: Rapier、ボクセル、OSM。箱の街 JSON と高さ場は既にある。

### 体（three-vrm の残り）

体は厚い。0.5 と使える週のあいだは後回しにしてよい（禁止ではない）。

- [ ] アバター 2 体のフレーム時間を README に書く（バス係数 1 のまま）
- [ ] ダンスの上に手だけ（VRMA + `ActionController` のレイヤ）
- [ ] 表情プリセットの一覧を `ActionController.names()` と同じ場所に

### ドキュメント

- [x] API 索引を Front / Shelf に分割
- [x] `KAGRA_ENGINE_GUIDE.md` を `docs/archive/` に移し、ルートはスタブだけ
- [x] 全体レビュー `docs/REVIEW.ja.md`

---

## Stage 1 — 楔の同時検証（頭脳 + 3 見本のあと。2 週間 × 最大 2 サイクル）

同じフォーマット（15〜30 行のスクリプト + 60 秒動画 + そのコミュニティの言葉で
書いた投稿文）を、打てる楔から投げる。最大 4 本。

| 楔 | 完成品の絵 | 投げ先 | 賭けている痛み | 今投げてよいか |
|---|---|---|---|---|
| **A: ローカル LLM に体を** | Ollama + VRM が 15 行で喋る | r/LocalLLaMA、HN | 「俺の LLM に顔がない」 | **Stage 0.5 のあと** |
| **B: 無人 3D AI VTuber** | 頭脳 + 歌 + ダンスの配信画面 | X、ニコニコ、VTuber 圏 | 「Live2D でなく 3D で無人配信したい」 | 頭脳のあと。絵は足りる |
| **C: VRoid マスコット** | 自分の VRoid がデスクトップ常駐で喋る | X 日本語圏、VRoid 界隈 | 「作ったアバターの置き場がない」 | 配布物。エンジン不足ではない。**ワンライナーを整えてから。優先は下** |
| **D: エージェントが作るゲーム** | Cursor / Claude が API 検索 → 実装 → ヘッドレス検証 → **遊べる**ゲーム | AI コーディング界隈（X、HN） | 「エージェントは画面が見えない」 | **3 見本 + D-6 のあと。** 箱部屋 3 本はループの証明で、投稿用の天井ではない |

動画の作法（全楔共通）:

- 最初の 3 秒に一番いいカット。つかみに機能説明を置かない
- 最後は「`pip install kagra` / `python -m kagra`」の 2 行だけ
- README 先頭に 10 秒 GIF
- 楔 D はログとリザルトがコンテンツでよい。見本が初日なら「箱を避けました」以上にならない
- [x] D-0 〜 D-5: 導線、Orb Rush 公開 API、Heart Catch / Switch Room / Dodge Room（ログ付き）
- [ ] D-6: 使える週の API。箱の焼き直しではない。30 秒以上 + スコアかゴール。3 見本のあと

**判定**: 2 週間で「動いた」報告 3 件以上の楔が勝ち。全滅なら切り口の言葉を変えて 1 回だけ再試行。
全滅 × 2 サイクルで Stage 6 へ。見本が初日のままだと「需要が無い」とは読まない。

Stage 1 終了の主役はスター数ではなく、質的な「動いた」とオーガニック install の有無。

## Stage 2 — 勝者をコード不要の完成品に

- 勝ち楔を 1 コマンド + 設定ファイルに。VRM はドラッグ & ドロップ、
  頭脳は Ollama / kairi / OpenAI 互換を設定 1 行で切替
- **合格ライン**: README 通りに 10 分で「自分のアバターが自分の選んだ声で喋る」
- **本物のサイン**: プログラマ以外からの「動いた」報告が 1 件でも来ること

## Stage 3 — 旗艦の公開運用（動く広告）

勝ち楔の完成形を自分で運用して見せ続ける。

- 楔 B 勝ち: `kagra/autopilot.py`、セットリスト、チャット extra、セーフティ、
  スケジューラ、NDI → RTMP。セーフティが済むまで無人の**公開**配信はしない
- 楔 C 勝ち: 常駐マスコットの配布物
- 楔 D 勝ち: エージェント製ゲームのギャラリー（D-6 以降を 1 本含める）

**完了条件**: 旗艦経由の流入が、投稿していない日にも来ること。

## Stage 4 — 需要のある場所に「体」を供給する

- LangChain / エージェントフレームワークの「体」ツール化
- Discord ボットに顔を付けるテンプレ
- AIAvatarKit のレンダラーバックエンド / Open-LLM-VTuber ユーザーの 3D 乗り換え口
- kairi は条件付き推奨として棚に並べる
- **本物のサイン**: 相手側エコシステムに KAGRA の名前が出る

## Stage 5 — 複利

- 「動いた VRM」報告 → README ギャラリー → 投稿者が拡散
- 設定・セットリストの共有形式
- ブラウザデモ（kagra-shared / WebGPU）は裾野施策としてここで解禁
- **達成の定義**: オーガニック比率 50% 超、外部発コンテンツが毎月

ここが北極星の計測上の到達（第一想起が外から観測できる）。

## Stage 6 — 転進条件

Stage 1 を 2 サイクル回して全楔で引きがなければ、単独プロダクトの拡大は畳み、
供給側に専念する（AIAvatarKit バックエンド、verify / MCP の切り出し）。
Rust コアはどの道でも資産。使える週を飛ばして全滅したら、需要より見本を疑う。
頭脳を飛ばして A/B が黙ったら、需要より面を疑う。

## 計測（目標であって約束ではない）

Stage 1 は数値より「動いた」の質とオーガニック install の有無。スター / DL は参考。

| 指標 | Stage 1 終了 | Stage 3 終了 | Stage 5 |
|---|---|---|---|
| 「動いた」報告 | 3+（勝ち楔）。これが本番 | 10+（うち自作 VRM 5+） | 継続的 |
| オーガニック install | 投稿していない日に 1 でも | 30% | 50%+ |
| 外部発コンテンツ | 1 | 3 | 毎月 |
| GitHub Stars（参考） | 100 | 500 | 2,000 |
| PyPI 月間 DL（参考） | 500 | 3,000 | 10,000 |

## 製品の決めごと（ここは変えない）

- 汎用 2D エンジンとして pygame / pyxel の土俵で競わない
- 2D の `Entity` / tilemap / Tk エディタ / boids を Front に戻さない
- Live2D 対応（Open-LLM-VTuber の土俵に上がらない）
- Web レンダラで three-vrm と直接対決しない（ブラウザデモは Stage 5 の裾野）
- torch / 重量級モデルをコア依存に入れない（5MB が配布の武器）
- text-to-vrma / Irodori-TTS のベンダリング（レシピで繋ぐ）
- YouTube / Twitch の API キーをコアやログに置くこと
- 比較表で競合を貶すこと
- デモへの高解像度テクスチャ / アセット同梱（プロシージャル + ライト）
- `KAGRA_ENGINE_GUIDE.md` を現行仕様として増やす（履歴は `docs/archive/`。現行は README と API 索引）
- 箱部屋 4 本目を D-6 と呼ぶこと
- Windows は EventLoop 1 本。GPU は `kagra.verify` の subprocess

## 今のバーでは後回し（禁止ではない）

Rapier / ボクセル / OSM / 4 段 CSM / SSAO / ボリュメトリック /
kagra-core と kagra-shared のレンダラ統合 / カリング P9。
箱の街 JSON と高さ場タイルは既にある。必要になったら指示で足す。

## 決めごと

- **アセット**: WAV / VRMA は wheel に入れない
- **モデル規約**: デモ・動画は Alicia（クレジット付き）か自作 VRM。AvatarSample 系は pixiv 規約
- **無人配信**: Stage 3 のセーフティが済むまで公開しない
- **API キー**: 環境変数のみ
- **投稿の締め**: 毎回同じ 2 行（`pip install kagra` / `python -m kagra`）
- **正直さ**: 「◯◯はまだ無い」を残す。API があることを「three.js 級」「Ursina 級」と呼ばない。
  見知らぬ人テストを通るまで級を使わない
