[workspace]
resolver = "2"
members = [
    "kagra-core",
    "kagra-shared",
]
# kagra-core は Python 拡張。共有 lib は独立に wasm / NDK / iOS へ出す。
default-members = ["kagra-core", "kagra-shared"]

[profile.release]
opt-level = 3
lto = true
