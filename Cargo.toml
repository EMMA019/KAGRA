[workspace]
resolver = "2"
members = [
    "kagra-shared",
]
# kagra-core（旧エンジン wgpu 0.19）は old/kagra-core でスタンドアロンに
# ビルドする（pip デモ用。新本線には含めない）。
default-members = ["kagra-shared"]

[profile.release]
opt-level = 3
lto = true
